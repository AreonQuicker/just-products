# import * from './generated/prisma.graphql'

type Mutation {
  testMutation(name: String): User!
}

type Query {
  products(where: ProductWhereInput, orderBy: ProductOrderByInput, skip: Int, after: String, before: String, first: Int, last: Int): [Product]!
}